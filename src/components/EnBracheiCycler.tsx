// Minimal stub for EnBracheiCycler - only used in glossary mode, not triad mode
import React from 'react';

interface EnBracheiCyclerProps {
  darkMode?: boolean;
}

export function EnBracheiCycler({ darkMode = true }: EnBracheiCyclerProps) {
  // Stub component - not used in triad mode
  return null;
}
